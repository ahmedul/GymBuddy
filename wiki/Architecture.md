# 🏗️ Architecture

Technical architecture and design decisions for GymBuddy.

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              CLIENTS                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌──────────────┐    ┌──────────────┐    ┌──────────────┐             │
│   │   iOS App    │    │ Android App  │    │   Web App    │             │
│   │  (Expo/RN)   │    │  (Expo/RN)   │    │   (Future)   │             │
│   └──────┬───────┘    └──────┬───────┘    └──────┬───────┘             │
│          │                   │                    │                      │
│          └───────────────────┼────────────────────┘                      │
│                              │                                           │
│                          HTTPS/REST                                      │
│                              │                                           │
└──────────────────────────────┼──────────────────────────────────────────┘
                               │
┌──────────────────────────────┼──────────────────────────────────────────┐
│                         AWS CLOUD                                        │
├──────────────────────────────┼──────────────────────────────────────────┤
│                              ▼                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                  Application Load Balancer                       │   │
│   │                    (HTTPS termination)                           │   │
│   └─────────────────────────────┬───────────────────────────────────┘   │
│                                 │                                        │
│   ┌─────────────────────────────▼───────────────────────────────────┐   │
│   │                    ECS Fargate Cluster                           │   │
│   │  ┌─────────────────────────────────────────────────────────┐    │   │
│   │  │                   FastAPI Service                        │    │   │
│   │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │    │   │
│   │  │  │ Task 1  │ │ Task 2  │ │ Task 3  │ │ Task N  │       │    │   │
│   │  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │    │   │
│   │  │              (Auto-scaling 2-10 tasks)                   │    │   │
│   │  └─────────────────────────────────────────────────────────┘    │   │
│   └─────────────────────────────┬───────────────────────────────────┘   │
│                                 │                                        │
│                    ┌────────────┼────────────┐                          │
│                    │            │            │                          │
│                    ▼            ▼            ▼                          │
│   ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐          │
│   │   Amazon RDS    │ │   Amazon S3     │ │  ElastiCache    │          │
│   │   PostgreSQL    │ │   (Images)      │ │  (Sessions)     │          │
│   │   (Multi-AZ)    │ │                 │ │                 │          │
│   └─────────────────┘ └─────────────────┘ └─────────────────┘          │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## Tech Stack

### Backend

| Component | Technology | Version | Purpose |
|-----------|------------|---------|---------|
| Framework | FastAPI | 0.109 | Async REST API |
| Language | Python | 3.11+ | Backend logic |
| ORM | SQLAlchemy | 2.0 | Database access |
| DB Driver | asyncpg | 0.29 | Async PostgreSQL |
| Migrations | Alembic | 1.13 | Schema migrations |
| Auth | python-jose | 3.3 | JWT handling |
| Password | passlib | 1.7 | Bcrypt hashing |
| Validation | Pydantic | 2.5 | Request/response validation |

### Mobile

| Component | Technology | Version | Purpose |
|-----------|------------|---------|---------|
| Framework | React Native | 0.73 | Cross-platform UI |
| Platform | Expo | SDK 50 | Build & deployment |
| Language | TypeScript | 5.3 | Type safety |
| State | Zustand | 4.5 | Global state |
| Server State | React Query | 5.x | API caching |
| Navigation | React Navigation | 6.x | Screen navigation |
| HTTP | Axios | 1.6 | API requests |

### Infrastructure

| Component | Technology | Purpose |
|-----------|------------|---------|
| IaC | AWS CDK | Infrastructure as code |
| Compute | ECS Fargate | Serverless containers |
| Database | RDS PostgreSQL | Managed database |
| Load Balancer | ALB | Traffic distribution |
| Container Registry | ECR | Docker images |
| Secrets | Secrets Manager | Credentials |

---

## Database Schema

```
┌───────────────────┐       ┌───────────────────┐
│      users        │       │    friendships    │
├───────────────────┤       ├───────────────────┤
│ id (PK)           │◄──────│ requester_id (FK) │
│ email             │◄──────│ addressee_id (FK) │
│ hashed_password   │       │ status            │
│ name              │       │ created_at        │
│ photo_url         │       └───────────────────┘
│ bio               │
│ training_level    │       ┌───────────────────┐
│ visibility        │       │      groups       │
│ is_active         │       ├───────────────────┤
│ created_at        │◄──────│ owner_id (FK)     │
└───────────────────┘       │ name              │
         │                  │ description       │
         │                  │ is_public         │
         ▼                  └───────────────────┘
┌───────────────────┐                │
│ user_favorite_gyms│                │
├───────────────────┤                │
│ user_id (FK)      │       ┌────────▼──────────┐
│ gym_id (FK)       │       │   group_members   │
│ created_at        │       ├───────────────────┤
└───────────────────┘       │ group_id (FK)     │
         │                  │ user_id (FK)      │
         │                  │ role              │
         ▼                  └───────────────────┘
┌───────────────────┐
│       gyms        │
├───────────────────┤       ┌───────────────────┐
│ id (PK)           │◄──────│     sessions      │
│ name              │       ├───────────────────┤
│ address           │       │ id (PK)           │
│ latitude          │       │ title             │
│ longitude         │       │ gym_id (FK)       │
│ phone             │       │ creator_id (FK)   │
│ website           │       │ scheduled_at      │
│ is_custom         │       │ duration_minutes  │
│ created_by_id     │       │ visibility        │
└───────────────────┘       │ max_participants  │
                            │ is_cancelled      │
                            └───────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
        ┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐
        │session_participants│ │ session_exercises │ │   (invites)       │
        ├───────────────────┤ ├───────────────────┤ └───────────────────┘
        │ session_id (FK)   │ │ session_id (FK)   │
        │ user_id (FK)      │ │ name              │
        │ rsvp_status       │ │ sets              │
        │ checked_in        │ │ reps              │
        │ invited_by_id     │ │ duration_seconds  │
        └───────────────────┘ │ notes             │
                              │ order             │
                              └───────────────────┘
```

---

## API Design

### RESTful Endpoints

```
/api/v1/
├── auth/
│   ├── POST   /register     # Create account
│   ├── POST   /login        # Get JWT token
│   └── GET    /me           # Current user
│
├── users/
│   ├── GET    /me           # My profile
│   ├── PATCH  /me           # Update profile
│   └── GET    /{id}         # User profile
│
├── friends/
│   ├── GET    /             # List friends
│   ├── POST   /request      # Send request
│   └── POST   /requests/{id}/accept
│
├── groups/
│   ├── GET    /             # My groups
│   ├── POST   /             # Create group
│   └── PATCH  /{id}         # Update group
│
├── gyms/
│   ├── GET    /             # Search gyms
│   ├── POST   /             # Create gym
│   ├── GET    /{id}         # Gym details
│   └── POST   /favorites/{id}
│
└── sessions/
    ├── GET    /             # Session feed
    ├── POST   /             # Create session
    ├── GET    /{id}         # Session details
    ├── POST   /{id}/join    # Join session
    ├── POST   /{id}/rsvp    # Update RSVP
    └── POST   /{id}/exercises
```

### Authentication Flow

```
┌─────────┐                                    ┌─────────┐
│  Client │                                    │  Server │
└────┬────┘                                    └────┬────┘
     │                                              │
     │  POST /auth/register                         │
     │  {email, password, name}                     │
     │─────────────────────────────────────────────▶│
     │                                              │
     │  201 {id, email, name}                       │
     │◀─────────────────────────────────────────────│
     │                                              │
     │  POST /auth/login                            │
     │  {username, password}                        │
     │─────────────────────────────────────────────▶│
     │                                              │
     │  200 {access_token, token_type}              │
     │◀─────────────────────────────────────────────│
     │                                              │
     │  GET /users/me                               │
     │  Authorization: Bearer <token>               │
     │─────────────────────────────────────────────▶│
     │                                              │
     │  200 {user profile}                          │
     │◀─────────────────────────────────────────────│
     │                                              │
```

---

## Project Structure

```
GymBuddy/
├── backend/
│   ├── app/
│   │   ├── api/v1/          # Route handlers
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── social.py
│   │   │   ├── gyms.py
│   │   │   └── sessions.py
│   │   ├── core/            # Config & security
│   │   │   ├── config.py
│   │   │   └── security.py
│   │   ├── crud/            # Database operations
│   │   ├── db/              # Database connection
│   │   ├── models/          # SQLAlchemy models
│   │   ├── schemas/         # Pydantic schemas
│   │   └── main.py          # FastAPI app
│   ├── alembic/             # Migrations
│   ├── tests/               # Pytest tests
│   ├── Dockerfile
│   └── requirements.txt
│
├── mobile/
│   ├── src/
│   │   ├── api/             # API client
│   │   ├── hooks/           # React Query hooks
│   │   ├── navigation/      # Navigation config
│   │   ├── screens/         # UI screens
│   │   └── store/           # Zustand stores
│   ├── App.tsx
│   └── package.json
│
├── infra/
│   ├── lib/
│   │   ├── network-stack.ts
│   │   ├── database-stack.ts
│   │   └── api-stack.ts
│   └── package.json
│
├── wiki/                    # Documentation
├── postman/                 # API collection
├── docker-compose.yml       # Local dev
└── README.md
```

---

## Security

### Authentication
- **JWT tokens** with configurable expiration
- **Bcrypt** password hashing (work factor 12)
- **OAuth2** password flow

### API Security
- **CORS** configured for allowed origins
- **Rate limiting** (planned)
- **Input validation** via Pydantic
- **SQL injection protection** via SQLAlchemy ORM

### Data Privacy
- **Profile visibility** controls
- **Friend-only** content options
- **GDPR-ready** data export (planned)

---

## Performance Considerations

### Database
- Indexed columns for common queries
- Async database connections
- Connection pooling

### API
- Async request handling
- Response pagination
- Efficient eager loading

### Mobile
- React Query caching
- Optimistic updates
- Image lazy loading

---

*See [API Reference](API-Reference.md) for detailed endpoint documentation.*
